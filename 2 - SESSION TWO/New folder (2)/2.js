// 2. Take an array with 5 colours. Create another array by adding 2 more colours to it.

const colors = ['red', 'yellow', 'blue', 'green', 'orange']

const morecolor = [...colors, 'a', 'b'];

console.log(colors)
console.log(morecolor)